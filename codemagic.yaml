# codemagic.yaml

workflows:
  main:
    name: Build and Package
    scripts:
      - name: Install Dependencies
        script: |
          npm install -g expo-cli
          npm install
      - name: Build IPA
        script: |
          npx expo prebuild -p ios
      
    artifacts:
      - ios/build/**
